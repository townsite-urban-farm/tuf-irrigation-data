{
  "_note": "Set grant_zone to the 0-based station index of 'Farm: outside'. Run scripts/fetch_log.py once and inspect data/daily/YYYY-MM-DD.json to see the station_names list and identify the correct index. Labels override the names reported by the controller; omit an entry to use the controller name.",
  "grant_zone": 0,
  "labels": {
    "0": "UA advancing markets"
  }
}
